import api from './http-common'

export const InventoryService = {
  // Ejemplos de endpoints para cuando tengas backend
  async getItems() {
    // const { data } = await api.get('/items')
    // return data
    return null
  },
  async createItem(payload) {
    // const { data } = await api.post('/items', payload)
    // return data
    return null
  },
}